---
import { Icon } from 'astro-icon/components'
import Layout from '../layouts/Layout.astro'

interface Tool {
  name: string
  description: string
  url?: string
}

interface ToolCategory {
  category: string
  tools: Tool[]
}

interface ReadingItem {
  title: string
  author?: string
  url?: string
  type: 'book' | 'article' | 'video'
}

const currentlyReading: ReadingItem[] = [
  // TODO(human): Add your currently reading items here
  // Example:
  // { title: 'Vue.js Design Patterns and Best Practices', author: 'Pablo David Garaguso', type: 'book' },
]

const toolCategories: ToolCategory[] = [
  {
    category: 'ソフトウェア',
    tools: [
      { name: 'Visual Studio Code', description: 'メインエディタ', url: 'https://code.visualstudio.com/' },
      { name: 'Arc', description: 'メインブラウザ', url: 'https://arc.net/' },
      { name: 'Raycast', description: 'ランチャー・生産性ツール', url: 'https://www.raycast.com/' },
      { name: 'Rectangle', description: 'ウィンドウ管理', url: 'https://rectangleapp.com/' },
    ],
  },
  {
    category: '開発ツール',
    tools: [
      { name: 'Ghostty', description: 'GPU アクセラレーテッド ターミナルエミュレータ', url: 'https://ghostty.org/' },
      { name: 'Starship', description: 'クロスシェル対応プロンプト', url: 'https://starship.rs/' },
      { name: 'mise', description: 'ランタイムバージョンマネージャー', url: 'https://mise.jdx.dev/' },
    ],
  },
  {
    category: 'ハードウェア',
    tools: [
      { name: 'MacBook Pro 13インチ M2 (2022)', description: 'メイン開発マシン (16GB)' },
      { name: 'LG 34WP60C-B', description: 'ウルトラワイドモニター' },
      { name: 'HHKB Professional HYBRID Type-S', description: '英語配列キーボード' },
      { name: 'Anker Soundcore Space One', description: 'ノイズキャンセリングヘッドフォン' },
      { name: 'UGREEN Revodok Pro', description: 'USB-C ドック' },
      { name: 'BenQ ScreenBar Pro', description: 'モニターライト' },
    ],
  },
]
---

<Layout title="About" description="Naoki Haba のプロフィールと開発環境">
  <div class="mx-auto px-4 py-16" style="max-width: var(--container-max);">
    <section class="mx-auto" style="max-width: 1200px;">
      <!-- Profile Header -->
      <div class="mb-20 flex flex-col items-center gap-8 text-center">
        <div class="relative">
          <div class="absolute inset-0 blur-3xl opacity-30" style="background: var(--gradient-primary);"></div>
          <img
            src="/Avatar 381x381.png"
            alt="Naoki Haba"
            width="160"
            height="160"
            class="relative size-40 rounded-full object-cover ring-4 ring-offset-4"
            style="ring-color: var(--color-border); ring-offset-color: var(--color-surface);"
            loading="lazy"
          />
        </div>
        <div>
          <h1 class="gradient-text mb-4 text-5xl font-extrabold" style="letter-spacing: -0.03em;">Naoki Haba</h1>
          <p class="text-2xl mb-6" style="color: var(--color-text-secondary);">💚 Front-end Developer loving Vue ecosystem</p>
          <div class="flex justify-center gap-4">
            <a
              href="https://x.com/naokihaba"
              target="_blank"
              rel="noopener noreferrer"
              aria-label="X (Twitter)"
              class="rounded-full p-3 transition-all hover:scale-110"
              style="background-color: var(--color-surface-elevated); color: var(--color-text-primary);"
            >
              <Icon name="lucide:twitter" class="size-6" />
            </a>
            <a
              href="https://github.com/naokihaba"
              target="_blank"
              rel="noopener noreferrer"
              aria-label="GitHub"
              class="rounded-full p-3 transition-all hover:scale-110"
              style="background-color: var(--color-surface-elevated); color: var(--color-text-primary);"
            >
              <Icon name="lucide:github" class="size-6" />
            </a>
            <a
              href="https://my-pull-requests.naoworks.workers.dev/"
              target="_blank"
              rel="noopener noreferrer"
              aria-label="My Pull Requests"
              class="rounded-full p-3 transition-all hover:scale-110"
              style="background-color: var(--color-surface-elevated); color: var(--color-text-primary);"
            >
              <Icon name="lucide:git-pull-request" class="size-6" />
            </a>
          </div>
        </div>
      </div>

      <!-- Currently Reading -->
      {currentlyReading.length > 0 && (
        <div class="mb-20">
          <h2 class="gradient-text mb-8 text-center text-4xl font-bold">現在読んでいる</h2>

          <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
            {currentlyReading.map((item) => {
              const Icon_name = item.type === 'book' ? 'lucide:book-open' : item.type === 'article' ? 'lucide:file-text' : 'lucide:video'
              return item.url ? (
                <a
                  href={item.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="group block rounded-xl p-6 transition-all hover:-translate-y-1 hover:shadow-lg"
                  style="background-color: var(--color-surface-elevated); border: 1px solid var(--color-border);"
                >
                  <div class="mb-3 flex items-center gap-2">
                    <Icon name={Icon_name} class="size-5" style="color: var(--color-primary);" />
                    <span class="text-xs font-semibold uppercase tracking-wider" style="color: var(--color-text-secondary);">
                      {item.type}
                    </span>
                  </div>
                  <h4 class="font-bold mb-2 transition-colors group-hover:gradient-text" style="color: var(--color-text-primary);">
                    {item.title}
                  </h4>
                  {item.author && (
                    <p class="text-sm" style="color: var(--color-text-secondary);">
                      {item.author}
                    </p>
                  )}
                </a>
              ) : (
                <div class="rounded-xl p-6" style="background-color: var(--color-surface-elevated); border: 1px solid var(--color-border);">
                  <div class="mb-3 flex items-center gap-2">
                    <Icon name={Icon_name} class="size-5" style="color: var(--color-primary);" />
                    <span class="text-xs font-semibold uppercase tracking-wider" style="color: var(--color-text-secondary);">
                      {item.type}
                    </span>
                  </div>
                  <h4 class="font-bold mb-2" style="color: var(--color-text-primary);">
                    {item.title}
                  </h4>
                  {item.author && (
                    <p class="text-sm" style="color: var(--color-text-secondary);">
                      {item.author}
                    </p>
                  )}
                </div>
              )
            })}
          </div>
        </div>
      )}

      <!-- Tools & Hardware -->
      <div>
        <h2 class="gradient-text mb-8 text-center text-4xl font-bold">開発環境</h2>

        <div class="space-y-12">
          {
            toolCategories.map((category) => (
              <div>
                <h3 class="mb-6 text-2xl font-bold flex items-center gap-3" style="color: var(--color-text-primary);">
                  <span class="h-1 w-8 rounded-full" style="background: var(--gradient-primary);" />
                  {category.category}
                </h3>
                <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
                  {category.tools.map((tool) =>
                    tool.url ? (
                      <a
                        href={tool.url}
                        target="_blank"
                        rel="noopener noreferrer"
                        class="group block rounded-xl p-6 transition-all hover:-translate-y-1 hover:shadow-lg"
                        style="background-color: var(--color-surface-elevated); border: 1px solid var(--color-border);"
                      >
                        <h4 class="font-bold mb-2 transition-colors group-hover:gradient-text" style="color: var(--color-text-primary);">
                          {tool.name}
                        </h4>
                        <p class="text-sm" style="color: var(--color-text-secondary);">
                          {tool.description}
                        </p>
                      </a>
                    ) : (
                      <div class="rounded-xl p-6" style="background-color: var(--color-surface-elevated); border: 1px solid var(--color-border);">
                        <h4 class="font-bold mb-2" style="color: var(--color-text-primary);">
                          {tool.name}
                        </h4>
                        <p class="text-sm" style="color: var(--color-text-secondary);">
                          {tool.description}
                        </p>
                      </div>
                    ),
                  )}
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </section>
  </div>
</Layout>
