---
import { Icon } from 'astro-icon/components';
import Layout from '../layouts/Layout.astro';
---

<Layout title="404 - ページが見つかりません" description="お探しのページは存在しません">
  <div class="mx-auto px-4 py-32" style="max-width: var(--container-max);">
    <div class="mx-auto text-center" style="max-width: 600px;">
      <!-- 404 Number with Gradient -->
      <div class="relative mb-12">
        <div class="absolute inset-0 blur-3xl opacity-30" style="background: var(--gradient-primary);"></div>
        <h1 class="relative gradient-text text-9xl font-black" style="letter-spacing: -0.05em;">
          404
        </h1>
      </div>

      <!-- Message -->
      <div class="mb-12">
        <h2 class="mb-4 text-4xl font-bold" style="color: var(--color-text-primary); letter-spacing: -0.03em;">
          ページが見つかりません
        </h2>
        <p class="text-xl" style="color: var(--color-text-secondary); line-height: 1.75;">
          お探しのページは存在しないか、移動した可能性があります。
        </p>
      </div>

      <!-- Navigation Cards -->
      <div class="grid gap-4 md:grid-cols-3 mb-12">
        <a
          href="/"
          class="group block rounded-xl p-6 transition-all hover:-translate-y-1 hover:shadow-xl"
          style="background-color: var(--color-surface-elevated); border: 1px solid var(--color-border);"
        >
          <Icon name="lucide:home" class="size-10 mx-auto mb-3" style="color: var(--color-primary);" />
          <h3 class="font-bold mb-2 transition-colors group-hover:gradient-text" style="color: var(--color-text-primary);">
            ホーム
          </h3>
          <p class="text-sm" style="color: var(--color-text-secondary);">
            トップページへ
          </p>
        </a>

        <a
          href="/talks"
          class="group block rounded-xl p-6 transition-all hover:-translate-y-1 hover:shadow-xl"
          style="background-color: var(--color-surface-elevated); border: 1px solid var(--color-border);"
        >
          <Icon name="lucide:presentation" class="size-10 mx-auto mb-3" style="color: var(--color-accent);" />
          <h3 class="font-bold mb-2 transition-colors group-hover:gradient-text" style="color: var(--color-text-primary);">
            Talks
          </h3>
          <p class="text-sm" style="color: var(--color-text-secondary);">
            登壇資料を見る
          </p>
        </a>

        <a
          href="/about"
          class="group block rounded-xl p-6 transition-all hover:-translate-y-1 hover:shadow-xl"
          style="background-color: var(--color-surface-elevated); border: 1px solid var(--color-border);"
        >
          <Icon name="lucide:user" class="size-10 mx-auto mb-3" style="color: var(--color-primary);" />
          <h3 class="font-bold mb-2 transition-colors group-hover:gradient-text" style="color: var(--color-text-primary);">
            About
          </h3>
          <p class="text-sm" style="color: var(--color-text-secondary);">
            プロフィール
          </p>
        </a>
      </div>

      <!-- Back Button -->
      <button
        onclick="history.back()"
        class="inline-flex items-center gap-2 rounded-xl px-6 py-3 font-semibold transition-all hover:scale-105 hover:shadow-lg"
        style="background: var(--gradient-primary); color: white;"
      >
        <Icon name="lucide:arrow-left" class="size-5" />
        <span>前のページに戻る</span>
      </button>
    </div>
  </div>
</Layout>
